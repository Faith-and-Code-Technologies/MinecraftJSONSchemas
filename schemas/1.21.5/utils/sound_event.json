{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "sound_id": {
            "type": "string",
            "title": "Sound ID",
            "description": "A resource location that points to a sound file."
        },
        "range": {
            "type": "number",
            "title": "Range",
            "description": "The fixed range of the sound."
        }
    },
    "required": ["sound_id"]
}